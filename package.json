{
  "name": "nxcoder",
  "version": "1.0.0",
  "type": "module",
  "main": "server.js",
  "dependencies": {
    "@google/genai": "^2.11.0",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "express": "^4.19.2",
    "markdown-it": "^14.2.0",
    "mime": "^4.0.3",
    "multer": "^1.4.5-lts.1",
    "puppeteer": "^23.0.0",
    "sharp": "^0.33.4",
    "sqlite3": "^5.1.7",
    "ws": "^8.17.1"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  },
  "scripts": {
    "start": "cd backend && go run ./cmd/server",
    "generate-icons": "node src-tauri/generate_icons.js",
    "prepare:dist": "mkdir -p dist && cp -rf index.html cdn deviceControl.js manifest.json sw.js webfonts assets dist/ 2>/dev/null || true",
    "build": "npm run prepare:dist && node src-tauri/generate_icons.js && npm run build:backend",
    "build:backend": "cd backend && go mod tidy && go build -o ../nxcoder-backend ./cmd/server",
    "pretauri:dev": "npm run prepare:dist && node src-tauri/generate_icons.js && npm run build:backend",
    "pretauri:build": "npm run prepare:dist && node src-tauri/generate_icons.js && npm run build:backend",
    "tauri": "tauri",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build",
    "tauri:android": "tauri android dev",
    "tauri:android:build": "tauri android build"
  }
}
